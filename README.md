# EmbedKit_Harshada_wani

Name: Harshada Wani

~Build Instructions:

bash:
gcc -Wall -std=c99 ringbuf.c -o ringbuf

Run the program:

./ringbuf


Modules:
ringbuf.c – Implements a fixed-size Ring Buffer with write, read, full, and empty operations.
